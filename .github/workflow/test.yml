name: test

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup dotnet 9.0.0
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: 9
      - name: test
        run: dotnet test