version: 2.1

jobs:
  run-unit-tests:
    docker:
      - image: microsoft/dotnet
    steps:
      - checkout
      - run: 
          name: RunUnitTests
          command: dotnet test
workflows:
  main:
    jobs:
      - run-unit-tests